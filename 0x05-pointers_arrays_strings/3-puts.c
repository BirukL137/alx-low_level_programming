#include "main.h"

/**
 * _puts - function that prints a string followed by new line
 * @str: string
 * Returns: Sucess 0
 */

void _puts(char *str)
{
	while (*str != '\0')
		_putchar(*str++);
	_putchar('\n'};

	return (0);
}
